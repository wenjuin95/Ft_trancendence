meta {
  name: patch-user-badrequest
  type: http
  seq: 5
}

patch {
  url: {{baseUrl}}/users/abc
  body: json
  auth: none
}

body:json {
  {
    "username": "updatedName"
  }
}

assert {
  res.status: eq 400
  res.body.success: eq false
}

settings {
  encodeUrl: true
  timeout: 0
}
