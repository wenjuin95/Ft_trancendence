meta {
  name: patch-user-duplicate-username
  type: http
  seq: 3
}

patch {
  url: {{baseUrl}}/users/1
  body: json
  auth: none
}

body:json {
  {
    "username": "username2"
  }
}

assert {
  res.status: eq 409
  res.body.success: eq false
  res.body.error: contains "Username already exists"
}
