meta {
  name: patch-friendship-success
  type: http
  seq: 2
}

patch {
  url: {{baseUrl}}/friendships/1/2
  body: json
  auth: inherit
}

body:json {
  {
    "status": "accepted"
  }
}

assert {
  res.status: eq 200
  res.body.success: eq true
}

settings {
  encodeUrl: true
}
