meta {
    name: "List Rooms"
    type: http
    seq: 2
}

get {
    url: {{baseUrl}}/rooms
    auth: inherit
}

assert {
    res.status: eq 200
    res.body: isArray
}

settings {
  encodeUrl: true
}